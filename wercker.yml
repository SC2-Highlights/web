box: wercker/nodejs@1
build:
    steps:
        - script:
            name: install bower
            code: sudo npm install -g bower
        - script:
            name: run bower install
            code: bower install